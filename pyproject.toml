[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "feed-client"
dynamic = ["version"]
description = ''
readme = "README.md"
requires-python = ">=3.8"
authors = []

[project.scripts]
feed-client = "feed_client.main:main"
